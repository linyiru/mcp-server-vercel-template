# =============================================================================
# MCP Server Configuration
# =============================================================================

# Server name displayed to MCP clients
# MCP_SERVER_NAME="My MCP Server"

# Server version
# MCP_SERVER_VERSION="1.0.0"

# =============================================================================
# Authentication (JWT / JWKS)
# =============================================================================

# OAuth issuer URL (must match the `iss` claim in JWT tokens)
# AUTH_ISSUER="https://auth.example.com"

# JWKS endpoint for JWT verification
# AUTH_JWKS_URL="https://auth.example.com/.well-known/jwks.json"

# Accepted JWT audiences (comma-separated)
# AUTH_AUDIENCES="https://mcp.example.com,https://api.example.com"

# Set to "true" to skip auth during local development
# AUTH_SKIP="true"

# =============================================================================
# Redis (optional, for MCP session persistence)
# =============================================================================

# REDIS_URL="redis://localhost:6379"

# =============================================================================
# Environment
# =============================================================================

# NODE_ENV="development"
